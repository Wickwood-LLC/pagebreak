name: PageBreak
type: module
description: "Adds the toolbar button which inserts horizontal page breaks. This feature is useful for setting document printing sections."
package: CKEditor
core_version_requirement: ^8 || ^9

dependencies:
  - ckeditor
  - fakeobjects